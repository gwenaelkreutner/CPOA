package testunitairemetier;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.fail;
import org.junit.Test;

import metier.Facture;


public class MFactureTest {
/*

	@Test
	public void testSimple() {
		try {
			Facture t = new Facture(1, 2, "06/06/1998");
		} catch (IllegalArgumentException e) {
			fail("Devrait renvoyer une erreur si problème de constructeur");
		}
		
	}

	// id_facture

	@Test
	public void testGetId_facture() {
		Facture f = new Facture(1, 1, 0403);
		assertEquals(1, f.getId_facture());
	}

	@Test
	public void testSetId_tvaFAIL() {
		try {
			Facture f = new Facture(-1, 1, 0403);

		} catch (IllegalArgumentException e) {
			fail("Devrait renvoyer une erreur car id_acture doit etre supérieur a 0");
		}
	}

	@Test
	public void testSetId_factureOk() {
		try {
			Facture f = new Facture(1, 1, 0403);

		} catch (IllegalArgumentException e) {
			fail("Renvoie pas d'erreur car id_facture > 0");
		}
	}

	// id_client

	@Test
	public void testGetId_client() {
		Facture f = new Facture(1, 1, 0403);
		assertEquals(1, f.getId_client());
	}

	@Test
	public void testSetId_clientFAIL() {
		try {
			Facture f = new Facture(1, -1, 0403);

		} catch (IllegalArgumentException e) {
			fail("Devrait renvoyer une erreur car id_client doit etre supérieur a 0");
		}
	}

	@Test
	public void testSetId_clientOk() {
		try {
			Facture f = new Facture(1, 1, 0403);

		} catch (IllegalArgumentException e) {
			fail("Renvoie pas d'erreur car id_client > 0");
		}
	}

	///// faire avec autre variable que int POUR DATE ///////
*/

}